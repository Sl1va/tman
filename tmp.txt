

shell:
    check config file and dirs
    call tman with command

Qs:
    1. shell.sh or tman.lua creates structure?
    2. What is shell.sh used for?
    3. Should tman.lua check that repos is not currepted?

As:
    1. tman.lua creates as git does
    2. To call tman.lua where it's installed at. That's it.
    3. Yes, git does it every time.
